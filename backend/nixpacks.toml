[phases.setup]
nixPkgs = ["python39", "tesseract", "poppler_utils", "pango", "gdk-pixbuf", "cairo", "gobject-introspection"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
